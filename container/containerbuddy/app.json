{
  "consul": "consul:8500",
  "services": [
    {
      "name": "steamleft",
      "port": 80,
      "health": "/usr/bin/curl --fail -s http://localhost:80",
      "poll": 10,
      "ttl": 25
    }
  ],
  "backends": [
    {
      "name": "app",
      "poll": 5
      // "onChange": "/opt/containerbuddy/reload-app.sh"
    }
  ]
}